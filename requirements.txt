# Recreate the nerfacc conda env package set (Python 3.10).
# Install torch/torchvision first from the CUDA 12.1 wheel index:
#   pip install torch==2.1.0+cu121 torchvision==0.16.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt
#   pip install -e . --no-build-isolation
#
# Build notes used on this machine:
#   pip install "setuptools<81" wheel ninja
#   export CC=gcc-11 CXX=g++-11 CUDAHOSTCXX=g++-11

# PyTorch (install via --index-url above; listed here for version pinning)
torch==2.1.0+cu121
torchvision==0.16.0+cu121
triton==2.1.0

# Build / packaging
setuptools==80.10.2
wheel==0.47.0
ninja==1.13.0
packaging==26.0

# nerfacc runtime deps
rich==15.0.0
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0

# Example / training deps
numpy==1.26.4
opencv-python==5.0.0.93
ImageIO==2.37.4
scipy==1.15.3
lpips==0.1.4
tqdm==4.67.3
pillow==12.2.0

# Torch transitive deps (pinned to current env)
filelock==3.29.0
fsspec==2026.4.0
Jinja2==3.1.6
MarkupSafe==3.0.3
networkx==3.4.2
sympy==1.14.0
mpmath==1.3.0
typing_extensions==4.15.0
requests==2.28.1
certifi==2022.12.7
charset-normalizer==2.1.1
idna==3.4
urllib3==1.26.13
